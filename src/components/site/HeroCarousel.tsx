import React from "react";

// Placeholder HeroCarousel component for FGCK site
const HeroCarousel = () => {
  return (
    <div className="w-full h-64 bg-blue-100 flex items-center justify-center rounded-lg">
      <span className="text-blue-700 text-2xl font-semibold">Hero Carousel Placeholder</span>
    </div>
  );
};

export default HeroCarousel;
